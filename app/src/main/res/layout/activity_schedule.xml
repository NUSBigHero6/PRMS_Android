<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:orientation="vertical"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:padding="16dp"
tools:context=".schedule.android.ui.ScheduleScreen">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Date"
        tools:text="Date of Program" />

    <EditText
        android:id="@+id/maintain_date_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="date"
        tools:text="Program Date" />
<TextView
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:text="ProgramName"
tools:text="Program Name" />

    <AutoCompleteTextView
        android:id="@+id/maintain_program_autocomplete_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Program Name" />

    <TextView
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:text="ProducerName"
tools:text="Producer Name" />

    <AutoCompleteTextView
        android:id="@+id/maintain_producer_autocomplete_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Producer Name"
        tools:text="Producer Name" />

    <TextView
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:text="PresenterName"
tools:text="Presenter Name" />

    <AutoCompleteTextView
        android:id="@+id/maintain_presenter_autocomplete_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Presenter Name"
        tools:text="Presenter Name" />

    <TextView
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:text="StartTime"
tools:text="Start Time" />

    <EditText
        android:id="@+id/maintain_starttime_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="time"
        android:maxLength="8"
        android:text="10:00"
        tools:text="10:00" />

<TextView
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:text="Duration"
tools:text="Duration" />

    <AutoCompleteTextView
        android:id="@+id/maintain_programslot_autocomplete_duration_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="00:30:00"
        tools:text="00:30:00" />

</LinearLayout>
